server:
  port: 1111
spring:
  application:
    name: eureka-server


  security:
    user:
      password: admin
      name: admin
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      defaultZone: http://localhost:1111/eureka